# SEG Learn Home — خطة تنفيذ داخلية مقيدة

**الحالة:** تخطيط معماري وبصري فقط — **لا يوجد تفويض للتنفيذ**  
**المرجع البصري الملزم:** `SEG_Learn_Approved_Reference.png` من `SEG_FINAL_VISUAL_ASSETS_PACKAGE_v1.0_PRODUCTION` [1]  
**قرار البنية:** لا يُنشأ مسار `/learn`. يبقى Learn قسماً داخلياً في `/dashboard` ويُفتح من رابط الشريط الحالي `#learn`. [2]

## حدود الخطة المعتمدة

تحافظ الخطة على Dashboard بوصفه المسار المحمي الوحيد. لا تتضمن هذه الوثيقة أي تعديل للواجهة أو المكونات أو ملفات CSS أو الاختبارات، ولا تفترض تغيير OAuth أو الجلسة أو قاعدة البيانات أو الأصول المنتجة أو الأسرار. الغرض منها هو تحديد أقل مسار تنفيذ بصري يمكن مراجعته لاحقاً بعد تفويض مستقل.

> **قاعدة التنفيذ المستقبلي:** يُعاد استخدام غلاف Dashboard والأصول المعتمدة أولاً، ثم يُعدّل فقط ما يتطلبه المرجع. لا تُقصّ صورة المرجع إلى بطاقات، ولا تُستبدل أي رسومات SEG المميزة بأيقونات عامة أو مخرجات مولّدة.

## موضع المكوّن الحالي والبنية المحفوظة

| الطبقة | الموضع الحالي | الوضع الحالي | دورها في التنفيذ المستقبلي |
|---|---|---|---|
| المسار المحمي | `client/src/App.tsx` | `/dashboard` هو شاشة Dashboard الوحيدة؛ لا يوجد `/learn`. | يبقى بلا تغيير. [3] |
| التنقل | `client/src/components/DashboardLayout.tsx` | عنصر **Learn** يستخدم `#learn` ويستدعي `scrollIntoView` داخل Dashboard. | يبقى هدف التنقل؛ لا ينشأ Router أو Route جديد. [2] |
| محتوى Learn | `client/src/pages/DashboardPage.tsx` | قسم داخلي بمعرّف `id="learn"` بعنوان **Upcoming Learning** وثلاثة عناصر تعلم مختصرة. | يتحول هذا الموضع نفسه إلى حاوية Learn الكاملة، مع عدم حذف My Home أو تحريك حدود المصادقة. [4] |
| الغلاف المشترك | `client/src/components/DashboardLayout.tsx` | شريط جانبي، ملف العرض، Dark Mode البصري، رأس الأدوات، وحالات المصادقة القائمة. | يعاد استخدامه كما هو؛ لا يعاد بناء auth أو rail. [2] |
| الرموز والسطوح | `client/src/index.css` | رموز SEG داكنة، بطاقات زجاجية، دعم الحركة المخفّضة، ومعالجات Dashboard. | توسعة صفوف Learn الموضعية فقط باستخدام رموز SEG المعتمدة. [5] |
| سجل الأصول | `client/src/lib/seg-assets.ts` | يعرّف مسارات الأصول المنشورة حالياً. | تضاف فقط مسارات أصول Learn التي يوافق مصدرها إنتاجياً وتُرفع عبر تخزين الويب المُدار. [6] |

## الأصول المطلوبة وحالة التوفر

تحتوي حزمة الإنتاج على الشعار الأساسي وشعار Hero والـ Brain/Heart Intelligence Core وتشريح بشري هولوغرافي وMedical AI وEmergency Response. لا تحتوي على تصديرات نظيفة ومفردة للرسوم الثمانية الخاصة ببطاقات Learn الظاهرة في المرجع. لذا فالأصل الأساسي للنواة متاح، بينما بقية الرسومات الخاصة بالوحدات **ليست مسموحة للاشتقاق من لقطة المرجع** وتبقى بوابة أصول قبل التنفيذ. [1] [6]

| عنصر المرجع | حالة الأصل في الإنتاج | استعمال محتمل لاحق | شرط حاسم قبل التنفيذ |
|---|---|---|---|
| شعار SEG وهوية الشريط | متاحان في سجل الأصول. | يعاد استخدامهما من الغلاف الحالي. | لا يلزم أصل جديد. |
| Knowledge Intelligence Core | يوجد أصل Brain/Heart Intelligence Core. | يعرض فقط بعد مطابقة تركيبته مع مرجع Learn. | تثبيت ملاءمته كتابةً؛ لا تُستخدم نسخة Dashboard تلقائياً إذا اختلف المشهد. |
| بطاقات Airway, Cardiac, Respiratory, Neurological | لا توجد تصديرات منفردة مسجلة. | بطاقات الوحدات المصورة. | تسليم/تأكيد أصول نظيفة معتمدة ومسارات ويب مُدارة. |
| بطاقات Fluid & Electrolytes, ECG, Trauma, Infectious Disease | لا توجد تصديرات منفردة مسجلة. | بطاقات الوحدات المصورة. | تسليم/تأكيد أصول نظيفة معتمدة ومسارات ويب مُدارة. |
| صورة الطبيب في الشريط الجانبي | لا توجد خريطة Learn خاصة مسجلة. | لا تعدّل في المرحلة الأولى؛ يعاد استخدام ملف العرض النصي القائم ما لم تعتمد صورة أصلية. | تفويض منفصل إذا طُلبت صورة. |
| أيقونات الفلتر/السهم/الأدوات العادية | مكوّنات أيقونات حالية متاحة. | عناصر تحكم غير مميزة فقط. | لا تستخدم بديلاً للأصول الطبية أو الهوية المميزة. |

## خطة التنفيذ البصري المقترحة

تُنفذ المراحل التالية فقط بعد تفويض **تنفيذ بصري داخلي** جديد وتأكيد بوابة الأصول. الترتيب يحافظ على `#learn` ويمنع تحويله إلى مسار أو صفحة مستقلة.

| المرحلة | التغيير المرئي المقترح داخل `#learn` | إعادة الاستخدام الإلزامية | لا يشمله التغيير |
|---|---|---|---|
| 0. بوابة الأصول والبنية | مطابقة كل رسم مرجعي مع أصل إنتاجي نظيف وتثبيت مخطط المحتوى الساكن. | حزمة الإنتاج، سجل `segAssets`، ومسارات التخزين المُدار. | لا رفع أو توليد أو قص أصول قبل التفويض. |
| 1. حاوية Learn الرئيسية | استبدال بطاقة **Upcoming Learning** الصغيرة بحاوية Learn ثلاثية المناطق داخل Dashboard نفسه: rail الحالي، canvas تعلم، وrail ثانوي. | `DashboardLayout` و`#learn` وكروت SEG الزجاجية. | لا Route، لا إعادة توجيه، لا تغيير أبعاد Rail المحفوظة في My Home. |
| 2. رأس وسطح المعرفة | عنوان Learn، النص المساند، شريط Emergency Action القائم، وKnowledge Intelligence Core مع الادعاء التعليمي الظاهر في المرجع فقط. | الرأس الحالي، نظام التنبيه الأحمر، ونواة إنتاجية ملائمة. | لا بيانات سريرية حية أو ادعاءات أداء من قاعدة بيانات. |
| 3. مستكشف الوحدات | تبويبات التصنيف، تحكم الفلترة المرئي، صفوف/شبكة ثماني بطاقات، أعداد وحدات ومستويات صعوبة مطابقة للمرجع. | بطاقات SEG والتباعد والرموز العادية. | لا رابط طبي/وصفة/Drug Reference ولا منطق تعلم غير مصرح. |
| 4. مسارات وموضوعات التعلم | شريط Learning Pathways ثم Popular Topics بالترتيب والمسافات المرجعية. | بطاقات dashboard الحالية حيث تتوافق. | لا إنشاء بيانات شخصية أو تقدم مستخدم. |
| 5. Rail التقدم الثانوي | Learning Overview، Study Streak، Continue Learning، Recommended For You كعناصر بصرية ساكنة وموسومة إن لزم. | أساليب progress والكروت والتأثير الزجاجي الحالية. | لا تغيير لبيانات التقدم الفعلية أو tRPC أو قاعدة البيانات. |
| 6. الاستجابة والتحقق | حل الشبكة إلى عمود واحد/اثنين في النطاقات الصغيرة مع الحفاظ على ترتيب التعلم وقابلية لوحة المفاتيح. | breakpoints والحركة المخفضة وحلقات focus القائمة. | لا تغيير لسلوك المصادقة أو التنقل المحمي. |

## الملفات المتأثرة عند تفويض التنفيذ فقط

| الملف | نوع الأثر المتوقع | سبب الإدراج |
|---|---|---|
| `client/src/pages/DashboardPage.tsx` | تعديل موضعي | استبدال جسم القسم `#learn` الحالي بمكوّن Learn داخلي وتقديم بيانات عرض ساكنة مطابقة للمرجع. |
| `client/src/components/LearnHomeSection.tsx` **(ملف جديد محتمل)** | مكوّن مرئي فقط | يُنشأ فقط إذا أصبح تركيب Learn كبيراً بما يكفي لتجنب تضخيم صفحة Dashboard؛ لا يسجل مساراً. |
| `client/src/components/DashboardLayout.tsx` | تعديل محتمل ومحدود | فقط إذا احتاج عنوان الرأس إلى إشارة القسم الداخلي النشط؛ لا تغيير لشرط `useAuth` أو التنقل أو logout. |
| `client/src/index.css` | تعديل موضعي | صفوف Learn للـ grid والسطوح والزينة والهولوغرام باستخدام رموز SEG الموجودة. |
| `client/src/lib/seg-assets.ts` | تعديل مشروط | فقط بعد توافر أصول Learn معتمدة ورفعها إلى التخزين المُدار. |
| `client/src/pages/DashboardPage.test.tsx` | اختبار | تحقق من وجود `#learn` والعناوين والمكوّنات المرئية المطلوبة مع عدم المساس بمسار Dashboard. |
| `client/src/components/DashboardLayout.test.tsx` | اختبار محتمل | فقط إذا تغير عنوان الرأس الداخلي؛ يُثبت عدم تغيير rail أو آلية المصادقة. |

**لن تتأثر:** `client/src/App.tsx`، أو `server/_core/oauth.ts`، أو `server/_core/cookies.ts`، أو `server/routers.ts`، أو `server/db.ts`، أو `drizzle/schema.ts`، أو الأسرار وبيئة الإنتاج.

## سجل المخاطر والضوابط

| الخطر | الأثر | الضابط المطلوب |
|---|---|---|
| إنشاء `/learn` لتقليد المرجع | خرق مباشر لبنية Dashboard المجمدة. | يبقى `#learn` الهدف الوحيد؛ يُرفض أي تعديل على `App.tsx` لهذا الغرض. |
| استخدام لقطة المرجع أو قصها كبطاقات | خرق سلطة الأصول وفقدان الاستجابة. | لا يبدأ تركيب شبكة البطاقات قبل أصول منفردة معتمدة. |
| استبدال الصور الطبية المميزة بأيقونات عامة أو مولّدة | مخالفة المطابقة الصارمة للمرجع. | تسليم/تأكيد أصل فردي لكل بطاقة أو إبقاؤها خارج نطاق التنفيذ. |
| تحويل أرقام التقدم النموذجية إلى بيانات مستخدم | تضليل أو خرق لحدود المنتج والبيانات. | تظل القيم مرئية ساكنة من المرجع فقط، ولا يضاف tRPC أو تخزين. |
| ضغط شاشة Learn في `DashboardPage` | صعوبة صيانة واحتمال أثر في My Home. | يفضل مكوّن داخلي منفصل عند الحاجة، مع إبقاء صفحة Dashboard منسقة. |
| تعارض rail الثانوي مع الشاشات الصغيرة | تمرير أفقي أو نص غير مقروء. | نقاط توقف مرجعية، ترتيب عمودي، اختبار لوحة مفاتيح وموبايل. |
| التباس الحالة الداكنة | Dark Mode الحالي عنصر بصري فقط. | لا يربط بأي حالة theme أو تخزين أو سلوك جديد ضمن تنفيذ Learn. |

## بوابة القبول قبل التنفيذ وبعده

لا يبدأ أي تنفيذ حتى تحصل الموافقات التالية: تفويض صريح لتنفيذ Learn الداخلي، قرار أصل مكتوب لكل بطاقة مرجعية، وتأكيد أن القيم المعروضة بصرية ساكنة وليست بيانات طبية أو حسابات مستخدم. بعد التنفيذ فقط، يلزم اختبار `#learn` داخل جلسة Dashboard مصادقة، التحقق من لوحة المفاتيح والاستجابة، Vitest محدث، فحص TypeScript، بناء إنتاج، ولقطات مقارنة مع مرجع Learn عند **1203 × 1308**. [1] [2]

> **حالة الخطة:** جاهزة للمراجعة والتنفيذ لاحقاً، لكنها لا تمنح تفويضاً برمجياً أو بصرياً بمفردها.

## المراجع

[1]: file:///tmp/seg-phase1-source/production/SEG_FINAL_VISUAL_ASSETS_PACKAGE_v1.0_PRODUCTION/SEG_FINAL_VISUAL_ASSETS_PACKAGE_v1.0_PRODUCTION/04_SCREEN_REFERENCES/04_LEARN/SEG_Learn_Approved_Reference.png "مرجع SEG Learn Home الإنتاجي المعتمد"

[2]: ./client/src/components/DashboardLayout.tsx "غلاف Dashboard والتنقل الداخلي الحالي"

[3]: ./client/src/App.tsx "تعريف المسارات الحالي"

[4]: ./client/src/pages/DashboardPage.tsx "قسم Learn الداخلي الحالي"

[5]: ./client/src/index.css "رموز وأنماط SEG/Dashboard الحالية"

[6]: ./client/src/lib/seg-assets.ts "سجل أصول SEG المنشورة حالياً"
