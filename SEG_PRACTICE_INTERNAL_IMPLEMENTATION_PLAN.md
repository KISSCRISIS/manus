# SEG Practice — خطة تنفيذ داخلية مقيدة

**الحالة:** تخطيط بصري وتقني فقط — **لا يوجد تفويض للتنفيذ**  
**المرجع البصري الملزم:** `SEG_Practice_Visual_Reference.png` ضمن `SEG_FINAL_VISUAL_ASSETS_PACKAGE_v1.0_PRODUCTION`، بأبعاد **1215 × 1295**. [1]  
**قرار البنية:** يبقى Practice قسماً داخلياً في `/dashboard` ويُفتح عبر `#practice`. لا يُنشأ مسار `/practice`. [2] [3]

## حدود الخطة المعتمدة

تحدد هذه الوثيقة مساراً بصرياً مستقبلياً فقط. لا تسمح بتعديل JSX أو CSS أو الاختبارات أو سجل الأصول أو إعدادات الإنتاج، ولا تغيّر OAuth أو الجلسات أو `useAuth` أو قاعدة البيانات أو الأسرار أو طبقات الخادم. كما تمنع صراحةً استخدام العناصر المؤقتة، والصور المولدة، والبدائل العامة للأصول الطبية المميزة، واقتصاص أجزاء من لقطة المرجع، وافتراض أي محتوى أو بيانات أداء للمستخدم.

> **قاعدة التنفيذ المستقبلي:** يُعاد استخدام غلاف Dashboard والأصول الإنتاجية الموثقة أولاً. عند غياب أصل مميز، يتوقف التنفيذ البصري للجزء المرتبط به حتى توفير أصل نظيف ومعتمد؛ لا يُستبدل به أصل مولّد أو عنصر نائب. [4]

لا يُدرج **Drug Reference**، حتى وإن ظهر كعنصر Quick Access في المرجع؛ إذ يبقى خارج نطاق المنتج المعتمد ولا تفسر هذه الخطة ذلك العنصر كتفويض لمحتوى دوائي أو وصفات أو مسار جديد.

## 1. موضع المكوّن الحالي والبنية المحفوظة

| الطبقة | الموضع الحالي | الحالة الفعلية | دورها في تنفيذ مستقبلي |
|---|---|---|---|
| المسار المحمي | `client/src/App.tsx` | `/dashboard` هو مسار Dashboard المحمي؛ لا يوجد مسار Practice مستقل. | يبقى دون تعديل. [3] |
| التنقل | `client/src/components/DashboardLayout.tsx` | عنصر **Practice** يستخدم `#practice` والتمرير الداخلي داخل Dashboard. | يبقى الهدف وطريقة التنقل كما هما. [2] |
| محتوى Practice | `client/src/pages/DashboardPage.tsx` | قسم `id="practice"` يعرض بطاقة **Quick Actions** مع أزرار أيقونية مضغوطة. | يكون هذا الموضع نفسه حاوية Practice الكاملة داخل Dashboard؛ لا ينشأ Route جديد. [5] |
| الغلاف المشترك | `client/src/components/DashboardLayout.tsx` | يوفر rail الداكن، الملف التعريفي النصي، Dark Mode البصري، عناصر الرأس، وحالات المصادقة الحالية. | يعاد استخدامه كما هو؛ لا تعديلات مصادقة أو rail بنيوية. [2] |
| نظام الواجهة | `client/src/index.css` | يحتوي رموز SEG الداكنة، كروت زجاجية، تباين cyan/red، وحركة مخفضة. | تضاف صفوف Practice الموضعية فقط بعد التفويض وباستخدام الرموز القائمة. [6] |
| سجل الأصول | `client/src/lib/seg-assets.ts` | يسجل أصول الهوية ونوى الدماغ/القلب والتشريح وMedical AI وEmergency Response. | يضاف إليه فقط أصل Practice فردي معتمد ومرفوع إلى التخزين المُدار. [7] |

## 2. الأصول المعتمدة المطلوبة وحالة توفرها

يمتلك المشروع أسس SEG القابلة لإعادة الاستخدام، لكنه لا يملك بعد خريطة أصول منفردة للمرئيات المميزة التي تؤلف المرجع: Hero Clinical Practice Environment، بطاقات Practice Mode الخمس، مصغرات النشاط، بطاقات Quick Practice، صور Skills Labs، وصورة الملف التعريفي المرجعية. [1] [7]

| عنصر المرجع | حالة الأصل الحالية | الاستخدام المستقبلي المحتمل | بوابة القبول قبل التنفيذ |
|---|---|---|---|
| شعار SEG وapp icon | متوفران ومسجلان. | يعاد استخدامهما في الغلاف القائم. | لا يتطلب أصل جديد. |
| Clinical Practice Environment hero | توجد أصول Brain/Heart ذات صلة، ومنها Core Dashboard النظيف. | يعرض فقط إذا أكد المصدر أنه مطابق لمشهد Practice المرجعي. | قرار ملاءمة أصل مكتوب؛ لا ينسخ أصل Dashboard تلقائياً. |
| خمس بطاقات Practice Mode | لا توجد مخرجات فردية نظيفة مسجلة. | Skill Trainer وCase Scenarios وSimulation Lab وAssessment وQuick Practice. | تسليم/تأكيد أصول منفردة معتمدة؛ لا عناصر مؤقتة. |
| مصغرات Recent Practice Activity وQuick Practice | لا توجد خريطة أصل مسجلة. | جداول النشاط وقوائم التدريبات السريعة. | تسليم/تأكيد صادرات نظيفة معتمدة. |
| صور Skills Labs السريرية | لا توجد صور منفردة مسجلة. | معرض المهارات السفلي. | تأكيد صور طبية معتمدة ونطاق استخدامها. |
| صورة الطبيب | توجد `landingProfileThumbnail`، بلا اعتماد صريح لـPractice. | عنصر profile المرجعي فقط. | اعتماد صريح لإعادة الاستخدام أو توفير صورة Practice أصلية. |
| الحالات العادية مثل lock/check/filter/chevron | ليست أصولاً طبية أو هوياتية مميزة بحسب المرجع. | تحكمات واجهة فقط. | يسمح باستعمال مكتبة الأيقونات الحالية للتحكم العادي لا كبديل للمرئيات الطبية. |
| Drug Reference Quick Access | غير مسموح حسب الحوكمة الحالية. | لا استخدام مستقبلي. | يحتاج استبدالاً آمناً معتمداً أو حذفه؛ لا إضافة محتوى دوائي. |

## 3. مراحل التنفيذ البصري المقترحة

المراحل الآتية لا تنفذ حالياً. تبدأ فقط بعد تفويض كتابة مستقل وتوافر الأصول المطلوبة وفق الجدول أعلاه، مع بقاء Practice في `#practice` داخل `/dashboard`.

| المرحلة | السطح البصري المطلوب | إعادة الاستخدام الإلزامية | خارج نطاق التغيير |
|---|---|---|---|
| 0. بوابة الأصول | إعداد خريطة مصدر لكل Hero وبطاقة وصورة مصغرة وصورة مهارة مرجعية. | حزمة الإنتاج، `segAssets`، التخزين المُدار. | لا رفع، توليد، أو قص أصول قبل اعتماد صريح. |
| 1. حاوية Practice | تحويل `#practice` من Quick Actions المصغرة إلى سطح Practice ثلاثي المناطق داخل محتوى Dashboard. | `DashboardLayout`، rail الحالي، ونظام SEG glass. | لا `/practice` ولا تعديل App routing. |
| 2. العنوان والـHero | عنوان **Practice**، النص المساند، Emergency Action القائم، وHero **Clinical Practice Environment** مع مؤشرات 48+/12/356/92% الواردة في المرجع. | رأس Dashboard، سطح الطوارئ الأحمر، وأصل Hero المعتمد فقط. | لا تُعامل القيم كبيانات مستخدم أو يتم ربطها بخادم. |
| 3. تحليلات Practice | rail يمين لـPractice Performance donut وSkills Overview وperiod selector بصري. | progress/card treatments الحالية. | لا tRPC أو قاعدة بيانات أو حساب أداء فعلي. |
| 4. Practice Mode | خمس بطاقات مرئية بالترتيب المرجعي مع أوصاف وأزرار Start ظاهرة فقط. | الكروت والحدود والتباعد من SEG؛ الأصول المعتمدة حصراً. | لا انطلاق سيناريو حقيقي أو منطق محتوى غير مفوض. |
| 5. النشاط والتوصيات | Recent Practice Activity، Quick Practice، Recommended For You، وشارات الحالة/المدة المرجعية. | الأنماط المشتركة للأيقونات والكروت فقط. | لا افتراض سجلات مستخدم أو توصيات حيّة. |
| 6. Skills Labs | بطاقات المهارات السريرية وصياغة More Skills وفق المرجع. | الأصول الطبية المورّدة والمعتمدة فقط. | لا صور placeholder أو مولدة. |
| 7. الاستجابة والتحقق | تكديس rail التحليلي والبطاقات بصرياً على الشاشات الضيقة مع الحفاظ على الترتيب واللوحة والمفاتيح. | نقاط التوقف والحركة المخفضة وحلقات التركيز القائمة. | لا تغيير في المصادقة أو بنية Dashboard. |

## 4. الملفات المتأثرة عند تفويض التنفيذ فقط

| الملف | نوع الأثر المتوقع | سبب الإدراج |
|---|---|---|
| `client/src/pages/DashboardPage.tsx` | تعديل موضعي | يستبدل جسم `#practice` الحالي بسطح Practice داخلي وتكوين بيانات العرض الساكنة المطابقة للمرجع. |
| `client/src/components/PracticeSection.tsx` **(ملف جديد محتمل)** | مكوّن بصري داخلي فقط | يعزل Hero، Practice Mode، Activity، Skills Labs، والـ rail لتجنب تضخم DashboardPage؛ لا يسجل Route. |
| `client/src/components/DashboardLayout.tsx` | تعديل محتمل ومحدود | فقط إن احتاج الرأس إلى إظهار القسم الداخلي النشط؛ لا تعديل لـ`useAuth` أو التنقل أو logout. |
| `client/src/index.css` | تعديل موضعي | صفوف grid، Hero، donut، skills، cards، وresponsive stacking باستخدام رموز SEG الحالية. |
| `client/src/lib/seg-assets.ts` | تعديل مشروط | فقط عند توفير أصول Practice الفردية المعتمدة ورفعها إلى التخزين المُدار. |
| `client/src/pages/DashboardPage.test.tsx` | اختبار | يثبت وجود `#practice` والعناوين والمناطق البصرية الأساسية من دون إضافة Route. |
| `client/src/components/DashboardLayout.test.tsx` | اختبار محتمل | فقط إذا تغير عنوان الرأس الداخلي؛ يثبت بقاء rail وحدود المصادقة دون تغيير. |

**لن تتأثر:** `client/src/App.tsx`، `server/_core/oauth.ts`، `server/_core/cookies.ts`، `server/routers.ts`، `server/db.ts`، `drizzle/schema.ts`، والأسرار أو إعدادات الإنتاج.

## 5. المخاطر التقنية وضوابطها

| الخطر | الأثر المحتمل | الضابط الإلزامي |
|---|---|---|
| إنشاء Route مستقل | يخالف مباشرة بنية Dashboard المجمدة. | يظل `#practice` الهدف الوحيد؛ لا يحرر `App.tsx` من أجل Practice. |
| غياب مرئيات Practice الطبية | يدفع نحو عناصر مؤقتة أو بدائل مولدة غير مسموحة. | يتوقف الجزء المرتبط إلى أن تصل صادرات إنتاجية معتمدة. |
| ربط مؤشرات المرجع ببيانات حقيقية | توسع غير مصرح لمنطق المنتج وادعاءات أداء غير موثقة. | تبقى المؤشرات مرئية ساكنة فقط، ولا يضاف backend أو tRPC. |
| إعادة إدخال Drug Reference | يخالف قرار الحوكمة وقيود المحتوى. | يحذف العنصر أو يستبدل فقط بتصميم آمن ومعتمد لاحقاً؛ لا محتوى دوائي. |
| تضخم DashboardPage | يزيد خطر رجوع My Home أو أقسام Dashboard الأخرى. | يفضل استخراج `PracticeSection` داخلياً بعد التفويض، مع اختبار My Home القائم. |
| كثافة الأعمدة على الشاشات الضيقة | قد تؤدي إلى تمرير أفقي أو نص غير مقروء. | مواصفات stacking واضحة، فحص narrow viewport، وترتيب focus صحيح. |
| صور سريرية غير مؤكدة | خطر استخدام أصل غير معتمد أو خارج نطاقه. | تحقق asset-by-asset قبل إدراجه في سجل الأصول. |
| Dark Mode البصري | ربطه بحالة فعلية يتجاوز نطاق التنفيذ المرئي. | يظل التحكم كما هو؛ لا theme state أو storage جديد. |

## 6. معايير التحقق والقبول

لا يبدأ التنفيذ قبل تحقق جميع الشروط: تفويض تنفيذ بصري منفصل، أصول Practice فردية معتمدة، وقرار واضح لحذف/استبدال Drug Reference، وتأكيد أن كل أرقام المرجع محتوى بصري ساكن. بعد التنفيذ فقط، يلزم تحقق `#practice` داخل جلسة Dashboard مصادقة، ومقارنة سطح المكتب عند **1215 × 1295**، ومراجعة شاشات ضيقة، وتنقل بلوحة المفاتيح، وحركة مخفضة، وVitest محدث، وفحص TypeScript، وبناء إنتاج. لا تصدر توصية تجميد بصري بلا دليل حي مصادَق عليه. [1] [2]

> **حالة الخطة:** جاهزة كمرجع مستقبلي فقط. التنفيذ معلق حتى اعتماد أصول Practice وتأكيد المراجع والتفويض الصريح.

## المراجع

[1]: file:///tmp/seg-phase1-source/production/SEG_FINAL_VISUAL_ASSETS_PACKAGE_v1.0_PRODUCTION/SEG_FINAL_VISUAL_ASSETS_PACKAGE_v1.0_PRODUCTION/04_SCREEN_REFERENCES/05_PRACTICE/SEG_Practice_Visual_Reference.png "مرجع SEG Practice الإنتاجي المعتمد"

[2]: ./client/src/components/DashboardLayout.tsx "غلاف Dashboard والتنقل الداخلي الحالي"

[3]: ./client/src/App.tsx "تعريف المسارات الحالي"

[4]: /home/ubuntu/skills/strict-reference-fidelity/SKILL.md "قواعد المطابقة الصارمة للأصول المعتمدة"

[5]: ./client/src/pages/DashboardPage.tsx "قسم Practice الداخلي الحالي"

[6]: ./client/src/index.css "رموز وأنماط SEG/Dashboard الحالية"

[7]: ./client/src/lib/seg-assets.ts "سجل أصول SEG المنشورة حالياً"
