# SEG My Pathway — خطة تنفيذ داخلية مقيدة

**الحالة:** تخطيط معماري وبصري فقط — **لا يوجد تفويض للتنفيذ**  
**المرجع البصري الملزم:** `SEG_MyPathway_Approved_Reference.png` من `SEG_FINAL_VISUAL_ASSETS_PACKAGE_v1.0_PRODUCTION`، بأبعاد **1197 × 1314**. [1]  
**قرار البنية:** يظل My Pathway قسماً داخلياً في `/dashboard` يُفتح عبر `#pathway`. لا يُنشأ مسار جديد. [2] [3]

## حدود الخطة المعتمدة

هذه وثيقة تخطيط فقط. لا تسمح بتعديل JSX أو CSS أو الاختبارات أو سجل الأصول أو إعدادات النشر. لا تشمل تغيير OAuth أو الجلسات أو `useAuth` أو البيانات أو قاعدة البيانات أو الأسرار. كما لا تسمح بإنشاء عنصر مرئي مؤقت، أو توليد أصل طبي/هوياتي بديل، أو استخراج بطاقات من لقطة المرجع، أو افتراض أن أرقام التقدم المعروضة هي بيانات مستخدم حقيقية.

> **قاعدة التنفيذ المستقبلي:** يُعاد استخدام غلاف Dashboard والأصول الإنتاجية المعتمدة فقط. يجب أن يكون لكل شعار أو دماغ أو قلب أو هولوغرام أو رسم مميز خريطة مصدر أصلية ومسار تخزين مُدار قبل إدراجه. [4]

## موضع المكوّن الحالي والبنية التي ستبقى محفوظة

| الطبقة | الموضع الحالي | الحالة الفعلية | دورها في تنفيذ مستقبلي |
|---|---|---|---|
| المسار المحمي | `client/src/App.tsx` | `/dashboard` هو مسار Dashboard المحمي؛ لا يُعرّف مسار My Pathway مستقل. | يبقى دون تعديل. [3] |
| التنقل | `client/src/components/DashboardLayout.tsx` | عنصر **My Pathway** يستخدم `#pathway` ويستدعي تمريراً داخلياً سلساً. | يبقى هذا الهدف وطريقة التنقل؛ لا يُضاف Router أو Route. [2] |
| محتوى Pathway | `client/src/pages/DashboardPage.tsx` | `section id="pathway"` يحتوي فقط بطاقة **Knowledge Mastery** وراداراً بأربعة تصنيفات. | يتحول هذا الموضع نفسه إلى حاوية My Pathway الكاملة داخل Dashboard، دون تحريك حدود المصادقة. [5] |
| غلاف التطبيق | `client/src/components/DashboardLayout.tsx` | يوفر rail الداكن، العنوان العلوي، إجراءات التنقل، الملف التعريفي، وDark Mode البصري. | يعاد استخدامه كما هو؛ لا يعاد بناء rail أو auth. [2] |
| الواجهة المشتركة | `client/src/index.css` | رموز SEG للخلفيات الداكنة، السطوح الزجاجية، والحركة المخفضة موجودة. | توسعات Pathway موضعية فقط باستخدام الرموز نفسها. [6] |
| الأصول المنشورة | `client/src/lib/seg-assets.ts` | يسجل شعار SEG، Core الدماغ/القلب، التشريح، ومشاهد طبية أخرى. | تضاف أصول Pathway فقط بعد اعتماد مصدرها إنتاجياً ورفعها إلى التخزين المُدار. [7] |

## الأصول المطلوبة وحالة التوفر

يوفر سجل الأصول الحالي شعار SEG والتطبيق، و`brainHeartCore`، و`dashboardBrainHeartCoreClean`، وهولوغرام التشريح البشري. لكن المرجع يتضمن Hero محدداً بعنوان **Your Journey to Excellence**، وصورة طبيب دائرية، وبطاقات مراحل تعليمية برسوم مميزة، وشارات إنجاز وميلستون. لا توجد خريطة أصول منفردة ومعتمدة لهذه العناصر Pathway-specific في السجل الحالي. [1] [7]

| عنصر المرجع | التوفر الحالي | الاستخدام المستقبلي المحتمل | بوابة الأصل قبل التنفيذ |
|---|---|---|---|
| هوية SEG والـ app icon | متوفران ومسجلان. | يعاد استخدامهما في rail المشترك. | لا يلزم أصل جديد. |
| Hero الدماغ/القلب | توجد أصول دماغ/قلب معتمدة ذات صلة. | تستخدم فقط إذا أكد المالك أنها المطابقة الدقيقة لمشهد Pathway. | قرار ملاءمة مكتوب؛ لا تُنقل نسخة Dashboard تلقائياً. |
| صورة Dr. Ahmed | `landingProfileThumbnail` متوفر، لكن لا يوجد تأكيد أنه أصل Pathway المرجعي. | لا تستخدم تلقائياً. | اعتماد صريح للاستخدام أو توفير صورة Pathway أصلية. |
| رسومات المراحل الست | لا توجد مخرجات منفردة مسجلة للكتب/التقييم/airway/cardiac/critical care/leadership كما تظهر في المرجع. | بطاقات الخط الزمني. | توفير أو تأكيد تصديرات نظيفة، منفردة، ومعتمدة. |
| Next Milestone وRecent Achievements | لا توجد أصول شارة/إنجاز مخصصة مسجلة. | rail التحليلي الأيمن. | توفير/تأكيد أصول أو اعتماد كتابة UI-state عادية حيث لا يكون الرسم المميز مطلوباً. |
| علامة القفل، التحقق، أسهم الفلتر، وواجهة الأرقام | لا يلزم لها أصل طبي/هوياتي مميز بحسب المرجع. | حالات UI العادية. | يُسمح بالمكتبة الحالية فقط للتحكم العادي، لا كبديل عن رسومات Pathway الطبية المميزة. [4] |

## خطة التنفيذ البصري المقترحة

تصف المراحل التالية عملاً مستقبلياً فقط بعد تفويض تنفيذ صريح واستكمال بوابة الأصول. تحافظ كل مرحلة على `#pathway` داخل `/dashboard`.

| المرحلة | المحتوى المرئي المطلوب داخل `#pathway` | إعادة الاستخدام الإلزامية | ما لا يتغير |
|---|---|---|---|
| 0. بوابة الأصول | بناء خريطة أصل لكل رسم Hero، مرحلة، إنجاز، وصورة ملف في المرجع. | حزمة الإنتاج و`segAssets` ومسارات التخزين المُدار. | لا رفع أو توليد أو قص قبل الموافقة. |
| 1. حاوية Pathway | توسيع قسم `#pathway` من بطاقة رادار إلى سطح Pathway ثلاثي المناطق داخل محتوى Dashboard. | `DashboardLayout` والـ rail الحالي ونظام الكروت الزجاجية. | لا `/pathway` ولا تغيير للمسارات. |
| 2. العنوان وHero | عنوان **My Learning Pathway**، النص المساند، Emergency Action القائم، Hero **Your Journey to Excellence** وإحصاءات 24/6/128 كما في المرجع البصري. | رأس Dashboard، سطح التنبيه الأحمر، وأصل Hero المعتمد فقط. | لا تُعامل الأرقام كبيانات شخصية ولا يضاف API. |
| 3. Learning Path | عنوان **Your Learning Path**، مرشح All Modules، خط زمني متصل، وست بطاقات حالات مكتملة/قيد التقدم/مقفلة مع أزرار Review/Continue/Locked. | رموز SEG والسطوح والكروت العادية. | لا يُبنى نظام تقدم أو صلاحيات أو فتح محتوى فعلي. |
| 4. rail التحليلي | Pathway Level، Pathway Progress، Next Milestone، Recent Achievements، وStudy Streak ضمن العمود الأيمن. | progress/card treatments الحالية. | لا قاعدة بيانات أو tRPC أو افتراض بيانات حقيقية. |
| 5. Pathway Overview | نطاق ملخص سفلي لـ Total Modules وCompleted وIn Progress وLocked وTotal Hours وCertificates. | أنماط ملخصات Dashboard الحالية. | لا حسابات استخدام أو اتصال backend. |
| 6. الاستجابة والتحقق | تحويل السطح إلى عمود/تكديس منطقي تحت نقاط التوقف مع الحفاظ على التسلسل والقراءة والتنقل بلوحة المفاتيح. | breakpoints والحركة المخفضة والتركيز الحالي. | لا تغيير لحالة المصادقة أو Dashboard-only architecture. |

## الملفات المتأثرة عند تفويض التنفيذ فقط

| الملف | نوع الأثر المتوقع | سبب الإدراج |
|---|---|---|
| `client/src/pages/DashboardPage.tsx` | تعديل موضعي | يستبدل جسم `#pathway` الحالي بمساحة My Pathway داخلية ويعرّف محتوى العرض الساكن المطابق للمرجع. |
| `client/src/components/MyPathwaySection.tsx` **(ملف جديد محتمل)** | مكوّن مرئي فقط | يفصل Hero والخط الزمني وrail التحليلي لتجنب تضخم DashboardPage، ولا يسجل مساراً. |
| `client/src/components/DashboardLayout.tsx` | تعديل محتمل ومحدود | فقط إن احتاج عنوان الرأس إلى تسمية القسم الداخلي النشط؛ لا تعديل لـ`useAuth` أو logout أو التنقل نفسه. |
| `client/src/index.css` | تعديل موضعي | صفوف Pathway للـ grid والخط الزمني والسطوح وحالات lock/progress باستخدام رموز SEG الحالية. |
| `client/src/lib/seg-assets.ts` | تعديل مشروط | فقط بعد قبول الأصول الفردية ورفعها إلى التخزين المُدار. |
| `client/src/pages/DashboardPage.test.tsx` | اختبار | يثبت وجود `#pathway` والعناوين الأساسية والـ timeline من دون تسجيل Route جديد. |
| `client/src/components/DashboardLayout.test.tsx` | اختبار محتمل | فقط عند تغيير العنوان الداخلي؛ يثبت بقاء rail والمصادقة دون تغيير. |

**لن تتأثر:** `client/src/App.tsx`، أو `server/_core/oauth.ts`، أو `server/_core/cookies.ts`، أو `server/routers.ts`، أو `server/db.ts`، أو `drizzle/schema.ts`، أو الأسرار وإعدادات الإنتاج.

## سجل المخاطر والضوابط

| الخطر | أثره | الضابط المطلوب |
|---|---|---|
| إنشاء Route مستقل لـ My Pathway | يخالف مباشرة بنية Dashboard المجمدة. | يبقى `#pathway` الهدف الوحيد؛ يرفض أي تعديل `App.tsx` لهذه الشاشة. |
| استعمال لقطة المرجع أو أجزاء منها كواجهة | يخالف مصدر الأصول ويكسر الاستجابة. | لا تُستخدم اللقطة كمنتج؛ يلزم أصل نظيف لكل رسم مميز. |
| إنشاء عناصر مؤقتة أو صور مولدة | يخالف قرار المستخدم والمطابقة الصارمة. | توقف التنفيذ عند غياب الأصل المعتمد بدلاً من الافتراض. |
| تحويل الإحصاءات المرجعية إلى بيانات حساب | تضليل للمستخدم أو توسيع غير مصرح لمنطق المنتج. | تبقى القيم بصرية ساكنة وموسومة عند الضرورة؛ لا backend. |
| التعارض مع My Home | قد يؤدي لتغيير غير مقصود في الترتيب أو أداء Dashboard. | يعزل السطح في `#pathway` أو مكوّن داخلي ويجري اختبار My Home القائم. |
| عدم توفر صورة الطبيب أو الأيقونات المميزة | يمنع ادعاء تطابق مرجعي. | اعتماد أصل واضح أو تسجيل الفجوة وعدم التنفيذ. |
| الازدحام على الشاشات الصغيرة | قد يسبب تمريراً أفقياً أو فقداناً للتسلسل. | تكديس rail التحليلي بعد Pathway، مع اختبار narrow viewport. |
| Dark Mode الحالي بصري فقط | ربطه بحالة فعلية يتجاوز نطاق التصميم. | يظل كما هو؛ لا تضاف حالة theme أو تخزين جديد. |

## بوابة القبول قبل التنفيذ وبعده

قبل البدء، يلزم تفويض كتابة صريح للتنفيذ الداخلي، وخريطة مصدر لكل أصل مميز، وتأكيد أن قيم المرجع محتوى بصري ساكن لا بيانات تعلم حقيقية. بعد التنفيذ، يلزم التحقق من التمرير عبر `#pathway` في جلسة Dashboard مصادقة، ومقارنة سطح المكتب عند **1197 × 1314**، وفحص استجابة ضيقة، وتنقل لوحة المفاتيح، واختبارات Vitest المحدثة، وفحص TypeScript، وبناء الإنتاج. لا يصدر قرار تجميد بصري دون دليل مصادَق عليه. [1] [2] [4]

> **حالة الخطة:** جاهزة للمراجعة المستقبلية، لكنها لا تسمح بتنفيذ برمجي أو بصري قبل اعتماد الأصول والتفويض المستقل.

## المراجع

[1]: file:///tmp/seg-phase1-source/production/SEG_FINAL_VISUAL_ASSETS_PACKAGE_v1.0_PRODUCTION/SEG_FINAL_VISUAL_ASSETS_PACKAGE_v1.0_PRODUCTION/04_SCREEN_REFERENCES/03_MY_PATHWAY/SEG_MyPathway_Approved_Reference.png "مرجع SEG My Pathway الإنتاجي المعتمد"

[2]: ./client/src/components/DashboardLayout.tsx "غلاف Dashboard والتنقل الداخلي الحالي"

[3]: ./client/src/App.tsx "تعريف المسارات الحالي"

[4]: /home/ubuntu/skills/strict-reference-fidelity/SKILL.md "قواعد المطابقة الصارمة للأصول المعتمدة"

[5]: ./client/src/pages/DashboardPage.tsx "قسم My Pathway الداخلي الحالي"

[6]: ./client/src/index.css "رموز وأنماط SEG/Dashboard الحالية"

[7]: ./client/src/lib/seg-assets.ts "سجل أصول SEG المنشورة حالياً"
